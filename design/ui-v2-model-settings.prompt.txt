Use the supplied UI image as the style reference. Create a NEW polished 2048 x 1536 landscape design board containing EXACTLY THREE equally sized Android screens showing SETTINGS and TWO DISTINCT MODEL PARAMETER PAGES for the simple Live Voice application. Preserve warm ivory backgrounds, charcoal text, terracotta primary actions, subtle rounded sand surfaces, clean Chinese sans-serif typography and generous touch targets. No perspective, hardware phones, floating objects, neon, dashboard charts or excessive card nesting. The board background is warm light gray. Screens are large, flat, front-facing and have the same geometry and margins as the reference. Each has Android status bar time 9:41. Small outer captions: "04 / 设置", "05 / 语音模型", "06 / 后端模型". No extra annotation text.

LEFT — SETTINGS ROOT
Header with back arrow and title "设置". First section heading "外观" followed by a three-item segmented selector with exact labels "跟随系统", "浅色", "深色". 跟随系统 is selected; the currently illustrated system is light. Next section heading "模型与连接". Three spacious list rows with refined outline icons and chevrons: row "语音模型" with smaller subtitle "GPT-Live-1"; row "后端模型" with subtitle "推理与搜索"; row "扫码更新配置" with subtitle "从电脑导入连接信息". Lower on screen a quiet section "会话" and one row "历史记录" with chevron. No login, subscriptions, unrelated preferences, security jargon, endpoint strings or visible credentials. No save button here because theme selection applies immediately. Keep the overall page sparse.

CENTER — GPT-LIVE-1 VOICE MODEL PARAMETERS
Back arrow and title "语音模型". Subtitle "GPT-Live-1". A low-emphasis compact connection summary with small olive status dot "已导入连接配置" and small outlined action "扫码更新". Below, a tidy row-based form with these exact labels and values: "模型 / 部署" with value "gpt-live-1"; "音色" with value "Marin" and chevron; "对话语言" with value "自动" and chevron; "会话时长" with value "10 分钟" and chevron. Next an expandable row "自定义指令" with chevron, collapsed (no giant text area). The imported model name is a read-only summary, not a secret. Near bottom show a small clear line "保存后，下次对话生效". Wide terracotta button "保存参数" at bottom with sufficient safe-area padding. No unsupported temperature or VAD sliders, no automatic reconnection action, no API key input, no actual endpoint, and no parameter overload.

RIGHT — BACKEND MODEL PARAMETERS
Back arrow and title "后端模型". Top row "启用后端模型" with toggle ON. Quiet compact status "已导入连接配置" with small outlined action "扫码更新". A read-only row "模型 / 部署" with illustrative value "自定义部署". Next form group "推理强度" with three options "低", "中", "高", 低 selected. Next an editable numeric input labeled "最大输出 Token" with value "1024". Row "联网搜索" with toggle ON. Small neutral helper copy directly beneath: "选项以模型支持为准". At bottom a separate line "保存后，下次对话生效" and wide terracotta button "保存参数". Do not add temperature, top-p, VAD, voice controls, cost estimates, or real model account data. This depicts an example deployment with these capabilities, not universal support.

All three screens must look realistically implementable, refined, consistent and uncluttered. Text must be large enough to read and exact. No elements may overflow, collide, or extend beyond screen boundaries. These are static design examples: actual model capabilities will be checked at runtime and connection details continue to arrive through encrypted QR import. Render only the specified screens, no extra pages or decorative text.
