# Live Voice 语音助手

用户通过手机或 Windows 客户端与语音助手自然交流，统一使用通用对话，可直接提出练口语等需求。每台设备独立连接模型服务并保留本机历史。

## Language

**对话**：
用户与助手的自然语音交流。日常提问和口语练习均通过同一入口开始，用户随时用语言提出当前需求。

**打断**：
助手说话期间，用户开始讲话并取得当前对话的话语权。

**连接配置**：
指定模型服务地址、访问凭据及模型或部署名称的一组信息。

**配置二维码**：
由 PC 生成、供手机导入连接配置的加密二维码。

**导入口令**：
生成与导入配置二维码时使用的同一段用户自定义字符串。
_Avoid_: 登录密码、API key

**历史会话**：
已经结束或已保存、可供用户回看的会话记录。

**会话名称**：
用于辨认历史会话的简短标题，可手动修改。启用后端时，新会话保存后可根据文字摘录自动命名；失败使用日期。删除会话前需要确认，Android 的文字与关联录音一并删除。

**会话录音**：
一次对话中用户与助手双方声音的可回放记录。新会话自动录制，结束后随本机历史会话查看和播放，保留到用户手动删除。

**手机联动**：
通过加密二维码把电脑中已保存的连接配置交给手机；不包含会话历史同步或跨设备接续对话。

**模型参数配置**：
用户为 GPT-Live-1 实时语音模型及可选后端模型调整会话行为的配置。它与连接配置、访问凭据是不同的概念。

**后端模型**：
为实时语音会话提供额外推理、计算或联网能力的可选模型服务。是否启用以及可调参数取决于实际服务能力。
